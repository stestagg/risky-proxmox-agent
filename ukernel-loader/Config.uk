menuconfig LIBUKERNEL_LOADER
	bool "ukernel-loader app"
	default y
	help
	  Console VM launcher unikernel for risky-proxmox-agent API.

if LIBUKERNEL_LOADER

config LIBUKERNEL_LOADER_API_BASE_URL
	string "Hard-coded API base URL"
	default "http://10.0.2.2:3000"
	help
	  Base URL used by the loader to call /api/vms, /api/launch and /api/host-shutdown.

endif
